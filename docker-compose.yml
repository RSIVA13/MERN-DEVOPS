version: "3.9"

services:
  ecommerce:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mern-ecommerce
    ports:
      - "5000:5000"
    env_file:
      - backend/.env
    restart: always